## v1.0.1 — Post-Publish Refresh Patch (2025-07-02)

### 🔧 Patch Update

- **Post-Publish Refresh Fix**
  - Ensures that after publishing a new article or note, the local data is immediately refreshed
  - Updates the SQLite database and in-memory dashboard state so the new event appears without requiring a manual reload
  - Tagged in both `continuum` and `continuum-pro` for consistency